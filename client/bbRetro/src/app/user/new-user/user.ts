export class NewUser {

    constructor(
        name: string,
        socketId?: string
    ) { }
}

export class Admin {

    constructor(
        name: string,
        socketId?: string
    ) { }
}